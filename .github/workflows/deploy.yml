name: Manual Deploy (Skeleton)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Pull latest backend image
        run: docker pull khawla/assessment-backend:latest

      - name: Pull latest frontend image
        run: docker pull khawla/assessment-frontend:latest

      - name: Placeholder – trigger Railway deployment
        run: |
          echo "This is a placeholder step. Railway deploy API call is intentionally not implemented."
          echo "When ready, add the API call here using appropriate secrets."


